# Version management for neuriplo-track project
# This file defines versions for all project dependencies

# External C++ Libraries (fetched via CMake FetchContent)
# ByteTrack-cpp publishes no tags; pinned to the head of main as of 2022-09-01,
# which is also the repository's most recent commit.
BYTETRACK_VERSION="d43805d461a714f65da039981bd5f5d21cf5cf59"
NEURIPLO_TASKS_VERSION="v0.8.1"
NEURIPLO_VERSION="v0.8.0"

# System Dependencies (minimum required versions)
OPENCV_MIN_VERSION="4.6.0"
GLOG_MIN_VERSION="0.6.0"
EIGEN3_MIN_VERSION="3.3.0"
CMAKE_MIN_VERSION="3.20"
